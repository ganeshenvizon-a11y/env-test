{
  "name": "envizon-blog-migration",
  "version": "1.0.0",
  "description": "Standalone utility to migrate legacy HTML blog pages into Headless WordPress as draft posts.",
  "type": "module",
  "private": true,
  "main": "migrate.js",
  "scripts": {
    "migrate:test": "node migrate.js --test",
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.5"
  }
}
